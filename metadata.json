{
  "model": "none-deterministic-policy-engine",
  "parameter_size": 0,
  "framework": "custom structured multi-agent pipeline (Python standard library)",
  "runtime": "Python 3.12",
  "policy_version": "EC_POLICY_V2",
  "notes": "No LLM is used to invent or calculate case facts. Agents perform read-only deterministic analysis over Olist CSV rows, followed by policy and evidence verification."
}
